import React from "react";
import { AppBar, Typography } from "@mui/material";

const Header = () => {
  return (
    <AppBar position="relative">
      <Typography variant="h2">North News</Typography>
    </AppBar>
  );
};

export default Header;
